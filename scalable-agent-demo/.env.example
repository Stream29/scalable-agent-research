# Copy this file to .env and replace the placeholder value.
ANTHROPIC_API_KEY=your_anthropic_api_key

# Optional: override the default chat session id.
# CHAT_SESSION_ID=claude-haiku-demo

# Optional: keep only the last N messages in memory.
# CHAT_MEMORY_WINDOW_SIZE=20
